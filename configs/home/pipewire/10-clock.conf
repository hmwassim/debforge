# PipeWire clock configuration for better audio stability
# Source: CachyOS-Settings, adapted for DebForge
context.properties = {
    default.clock.rate = 48000
    default.clock.quantum = 1024
    default.clock.min-quantum = 512
    default.clock.max-quantum = 2048
}
