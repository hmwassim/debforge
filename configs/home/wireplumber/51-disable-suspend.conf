# Disable suspend for audio devices to prevent crackling on wake
# Source: CachyOS-Settings, adapted for DebForge
monitor.alsa.rules = [
  {
    matches = [{ node.name = "~alsa_output.*" }]
    actions = { update-props = { session.suspend-timeout-seconds = "0" } }
  }
]
