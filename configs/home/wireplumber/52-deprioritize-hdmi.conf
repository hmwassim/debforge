# Deprioritize HDMI/DP audio outputs (they're usually not the default)
# Source: CachyOS-Settings, adapted for DebForge
match.node.name = "~alsa_output.*"
match.node.nick = "~HDMI.*|DP.*"
node.priority.session = 100
