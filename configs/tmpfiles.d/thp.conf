# THP defrag: defer+madvise — applications that explicitly request huge pages
# (via madvise) get them immediately; automatic defrag is deferred to avoid stalls.
# Source: CachyOS-Settings
w! /sys/kernel/mm/transparent_hugepage/defrag - - - - defer+madvise
