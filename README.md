# debforge

```sh
curl -sL https://raw.githubusercontent.com/hmwassim/debforge/main/inshall.sh | sudo bash
```

```
Usage: debforge <command>

Commands:
    --self-update       Update debforge
    --self-remove       Remove debforge
    --help              Show this help
    --version           Show version
```
