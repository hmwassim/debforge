name: nvidia
type: apt
packages:
  - nvtop
variants:
  open: nvidia-open
  proprietary: cuda-drivers
backports:
  - nvidia-vaapi-driver
configs:
  /etc/modprobe.d/99-nvidia.conf: |
    # NVIDIA kernel module tunables
    # PAT: better CPU memory management for GPU buffers (faster).
    # SkipMemoryZero: don't zero GPU allocations on alloc.
    # AggressiveVblank: lower-latency display-interrupt handling.
    # DynamicPowerManagement 0x02: allow dGPU idle power-down.
    options nvidia NVreg_UsePageAttributeTable=1
    options nvidia NVreg_InitializeSystemMemoryAllocations=0
    options nvidia NVreg_RegistryDwords=RmEnableAggressiveVblank=1
    options nvidia NVreg_DynamicPowerManagement=0x02
key_url: https://developer.download.nvidia.com/compute/cuda/repos/debian13/x86_64/8793F200.pub
key_path: /etc/apt/trusted.gpg.d/nvidia-cuda.gpg
key_dearmor: true
source_path: /etc/apt/sources.list.d/nvidia-cuda.list
sources: |
  deb https://developer.download.nvidia.com/compute/cuda/repos/debian13/x86_64/ /
