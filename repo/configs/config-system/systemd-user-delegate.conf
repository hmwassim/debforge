# By default systemd only delegates the memory and pids cgroup controllers
# to a user's session manager. This delegates cpu, cpuset, and io as well,
# so per-app resource limiting (GameMode-style CPU/IO shaping, systemd-run
# --user, etc.) actually has something to manage.
[Service]
Delegate=cpu cpuset io memory pids
