# Modern systemd already delegates cpu, memory, and pids to a user's
# session manager by default. This adds cpuset and io on top, so per-app
# resource limiting (GameMode-style CPU/IO shaping, systemd-run --user,
# etc.) actually has something to manage for those two controllers as well.
[Service]
Delegate=cpu cpuset io memory pids
