# Clear coredumps older than 3 days. This is an explicit policy, not just
# documentation of the default: upstream systemd's own retention window has
# varied between 3 days and 2 weeks across versions, so pin it rather than
# rely on whatever a given release ships.
d /var/lib/systemd/coredump 0755 root root 3d
